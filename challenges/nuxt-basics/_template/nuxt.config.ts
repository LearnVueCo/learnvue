// https://nuxt.com/docs/api/configuration/nuxt-config

// ⚠️ if you make any changes to this file, you must rerun `npm run dev` to make sure the tests are loading properly.

export default defineNuxtConfig({
  modules: ['nuxt-vitest']
})
